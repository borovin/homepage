{
  "name": "homepage",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "start": "ln -snf ./src/ public && grunt config && node server.js",
    "build": "grunt build",
    "test": "grunt test"
  },
  "dependencies": {
    "express": "^4.10.7",
    "grunt": "^0.4.5",
    "grunt-gitinfo": "^0.1.7",
    "grunt-shell": "^1.1.1",
    "phantomcss": "^0.10.0",
    "requirejs": "^2.1.16"
  }
}

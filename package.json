{
  "name": "homepage",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "rm -rf ./build && node ./tools/r.js -o ./tools/buildConfig.js && ln -snf ./build/ public",
    "bower": "./node_modules/bower/bin/bower install"
  },
  "dependencies": {
    "bower": "^1.3.12",
    "express": "^4.10.7"
  }
}
